<h1>Enter a new ingredient and does</h1>

<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient, collection: Ingredient.order(:name), prompt: "Choose an ingredient"%>
  <%= f.button :submit %>

<% end %>

